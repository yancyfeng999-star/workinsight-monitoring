# 产品合同（Product Contract）

> 版本：v1.1 · 日期：2026-08-10 · 状态：**部分决策已确认，多数为 working assumption；**
> 正式部署前须按《继续开发执行计划-审查整改到完整交付-v1.0.md》第 24 节重新确认。
> 确认状态见 `docs/product/decision-register.md`。本文件中的“确认结果”列仅记录当前工程
> 假设与开发默认值，不代表用户已授权的生产决定。

## 1. 主目的与用户问题

**主目的（唯一）：员工效率与工作节奏分析。**

组织缺少可信、低侵入的证据来回答：

- 办公电脑时间主要花在哪些应用和网站类别；
- 深度工作、沟通、会议、检索和频繁切换的模式是什么；
- 工具采用、流程瓶颈和异常使用发生在哪里；
- 如何给员工本人和管理者提供可行动的改进建议，而不是靠主观印象。

## 2. 目标用户与角色

| 角色 | 团队汇总 | 员工明细 | 策略 | 导出 | 审计 |
| --- | --- | --- | --- | --- | --- |
| Manager（团队主管） | 授权团队 | 授权团队 | 否 | 汇总 | 自己访问记录 |
| Company Admin | 组织汇总 | 按内部权限（需审计） | 是 | 受控 | 管理操作 |
| Internal Auditor | 必要汇总 | 按审计范围 | 否 | 只读 | 是 |
| System Operator | 仅健康数据 | 否 | 基础运维 | 否 | 运维审计 |

## 3. 决策与确认状态（15 项）

> 完整状态（confirmed / working_assumption / unconfirmed）见 `docs/product/decision-register.md`。
> 下表仅保留每项决策的工程默认值。未确认事项不阻塞合成数据开发，但不能被写成用户授权的生产决定。

| # | 决策点 | 工程默认值 | 状态 |
| --- | --- | --- | --- |
| 1 | 首要用途 | 员工效率与工作节奏分析（唯一主目的） | working_assumption |
| 2 | 设备-员工映射 | IT 提供清单，管理员在监控端手动映射 | working_assumption |
| 3 | 监控端部署 | 公司管理员 Mac 本机（内部使用，单机部署，后续可迁移内网服务器） | working_assumption |
| 4 | 明细 vs 汇总 | 两者都要；个人明细需角色授权且写入审计日志 | working_assumption |
| 5 | 窗口标题 | **默认关**（仅记录应用名）；策略开启且用户授予辅助功能权限时才采集，需脱敏 | working_assumption |
| 6 | 浏览数据精度 | 仅 `registrable_domain`（如 `example.com`）；path/query 默认不采 | working_assumption |
| 7 | 暂停权 | 仅管理员远程控制；员工侧无暂停入口；暂停状态如实上报，不伪造在线 | working_assumption |
| 8 | 保留期 | 配置必须为有限整数天，有产品默认值、硬上限、变更审计；"磁盘未满可 99 年"不作生产逻辑 | unconfirmed |
| 9 | 本地永不记录 | 密码管理器、银行/支付、个人邮箱、宗教/政府身份服务、localhost/内网管理页（管理员可增规则，不可取消私密模式硬规则） | working_assumption |
| 10 | 大模型 Provider | DeepSeek API（监控端调用；Provider 可切换；密钥只在 Worker 环境） | working_assumption |
| 11 | AI 报告受众 | 仅公司管理员 | working_assumption |
| 12 | SSO/MDM | 均无；MVP 用内置账号 + 手动分发 | working_assumption |
| 13 | 最低 OS | macOS 13+ / Windows 11（Win10 LTSC 单列兼容评估） | working_assumption |
| 14 | Pilot | 暂未确定，先完成技术薄切片（Phase 1）再定设备清单 | unconfirmed |
| 15 | 打包分发 | 本地可安装测试包优先；正式签名/公证/远端发布是独立门禁 | unconfirmed |

## 4. MVP 主路径

```text
管理员创建组织与采集策略
  → 生成一次性设备注册码
  → IT 在公司电脑安装 Agent 并完成首次配置
  → Agent 注册登录启动项并申请必要系统权限
  → 后续登录时 Agent 默认在后台启动，不弹主窗口
  → Chrome/Edge 扩展与 Agent 配对（Native Messaging）
  → 被监控端采集、字段过滤、区间合并和离线排队
  → 加密上传并获得幂等确认
  → 监控端 Worker 聚合、分类并形成规则事实
  → 监控端 DeepSeek 基于汇总生成带证据的分析
  → Web Console 展示个人/团队数据、设备状态和 AI 报告
  → 授权管理员可修正分类、导出或按内部策略删除数据
```

## 5. MVP 必须完成

- macOS/Windows 前台应用与使用时长；
- 用户登录后默认后台启动、单实例、崩溃有界恢复；
- 空闲、锁屏、睡眠与唤醒处理；
- Chrome/Edge 活动标签页域名与时长；
- 本地策略、敏感域名过滤、离线队列和重试；
- 设备注册、吊销、健康与版本；
- 组织/用户/团队/RBAC；
- 监控端日时间线、分类、团队汇总；
- 管理员审计日志、受控导出与数据清理；
- 监控端日/周 AI 分析（DeepSeek），输出可追溯到聚合统计；
- 自动测试保证 Agent 包不包含模型密钥、Prompt、模型权重或业务分析模块；
- macOS/Windows 本地可安装测试包；正式签名、公证与远端发布另设门禁。

## 6. MVP 明确不做（禁止）

- 截图、连续录屏、摄像头、麦克风；
- 逐键记录、键盘内容、剪贴板；
- 网页正文、DOM、表单输入、密码、Cookie；
- 邮件/聊天正文、文件内容、文件上传内容；
- 私密/无痕浏览（命中即丢弃，永不入队）；
- 网络抓包、中间人解密、HTTPS 内容拦截；
- 远程桌面、远程 Shell、任意脚本执行；
- 绕过 TCC/UAC/浏览器授权、伪装系统进程、未经签名的反卸载能力；
- 端侧分类、端侧统计报告、端侧大模型或模型 Provider 密钥。

## 7. 成功指标（Pilot 目标）

- Agent 7 日在线率 ≥ 98%；
- 用户登录后 30 秒内后台启动成功率 ≥ 99%；
- 正常登录启动不弹主窗口；关闭设置窗口后采集持续率 = 100%；
- 有效工作时段数据完整率 ≥ 95%；
- 上传 P95 延迟 < 5 分钟；
- 崩溃会话率 < 0.1%；
- 单设备网络 < 10 MB/工作日；
- 管理员对 AI 报告“有帮助”反馈 ≥ 60%，事实错误反馈 < 2%；
- 无未经授权的个人详情访问；无禁止字段进入服务端/模型的事件。

## 8. 场景排除（单独立项，不在本系统“顺便加”）

家长控制、BYOD 私人设备、涉密/金融/医疗/关键基础设施、录屏 OCR/通信审计/DLP、
远程控制与取证、基于行为的薪资/门禁/权限决策、外部多租户 SaaS。
