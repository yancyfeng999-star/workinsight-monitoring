# WorkInsight 独立审核计划：进度、前后端接入、用户体验与交付证据 v1.0

> 日期：2026-08-12
> 审核基线：fix/wave0-1 @ 5021f94
> 配套实施计划：后续实施计划-前后端真实接入与用户体验闭环-v2.0.md
> 审核性质：只读审查与验证，不代替实现 Agent 修代码
> 当前基线判定：FAIL，不能按“Task 1–8 已完成”进入 Task 9

## 0. 审核目的

本计划交给独立 Reviewer 执行，用于回答四个问题：

1. 实现进度是否与计划、README 和 delivery-status 一致；
2. 被监控端、API、数据库、Worker、Web Console、DeepSeek 是否真实接通；
3. 被监控端后台体验和监控端管理体验是否达到可用标准；
4. 测试、构建、运行、打包、发布和安装证据是否被准确区分。

Reviewer 的工作是找出事实并出具 PASS / FAIL / BLOCKED，不是在审核过程中顺手修改源代码。

## 1. 审核边界

### 1.1 允许

- 读取本项目目录内全部文件；
- 执行不会越出项目边界的静态检查、单元测试、集成测试和本地构建；
- 使用项目内合成数据；
- 启动项目内本地开发服务；
- 在项目内新增审核报告和合成截图；
- 检查本机已有工具和只读系统状态。

### 1.2 未经用户当次授权禁止

- 写入用户 Library、Applications 或系统目录；
- 注册或删除 macOS 登录项；
- 安装 Native Messaging Host 或浏览器扩展；
- 修改 Windows Registry、Task Scheduler 或服务；
- 注销、重启、锁屏或改变真实工作会话；
- 安装、升级、回滚或卸载应用；
- push、发布、远端部署；
- 调用付费或生产 DeepSeek；
- 使用真实员工、真实浏览历史或真实凭据。

因授权缺失无法执行的项目，结论必须是 BLOCKED，不得用代码审查、Mock 或 Mac 结果替代。

## 2. Reviewer 独立性与工作方式

1. 审核前记录目标 branch、HEAD、git status 和 commit range。
2. 不以提交信息、计划勾选、README 或实现 Agent 自述作为完成证据。
3. 每个关键结论至少有以下一种证据：
   - 运行输出；
   - 可定位源码路径和行号；
   - 数据库查询结果；
   - 浏览器/Tauri 可见结果；
   - 安装包或进程级证据。
4. 对真实链路，必须从生产入口追踪，不只审查独立 crate、helper 或 test double。
5. 先执行审核，再阅读实现 Agent 的总结，以减少锚定。
6. 审核发现问题后不改源码；写 finding 并退回实现 Agent。
7. 修复后只重测受影响 Gate 和必要回归，保留前后证据。
8. 所有报告只记录合成 ID、域名和账号。

## 3. 严重级别

| 级别 | 定义 | 处理 |
| --- | --- | --- |
| P0 | 核心链路不可用、数据静默丢失、身份/Scope 绕过、证据严重虚报 | 立即 FAIL，停止进入下一 Wave |
| P1 | 主要功能错误、可靠性/安全/体验导致产品不可交付 | 当前 Wave FAIL |
| P2 | 有明确影响但存在可接受临时绕行 | 可带条件进入下一小步，不可最终通过 |
| P3 | 文案、轻微视觉、一致性或维护性问题 | 记录并排期 |

## 4. 当前已知基线 Finding

独立 Reviewer 必须重新验证，不能直接复制本表。

| ID | 严重度 | 当前观察 | 预期修复后证据 |
| --- | --- | --- | --- |
| B-001 | P0 | Release Engine 不加载 enrollment 身份 | Release 进程重启后真实 identity 入队 |
| B-002 | P0 | Runtime 不调用 Uploader/Policy Client | 进程级上传和策略 E2E |
| B-003 | P0 | 产品路径使用 plain LocalStore | SQLite 无明文扫描 |
| B-004 | P0 | MemorySecretStore 与 secret_token=None | 重启后 token/queue key 恢复 |
| B-005 | P0 | Tauri windows 为空，首次 UI 不可达 | 首次窗口/后台窗口数量测试 |
| B-006 | P0 | Extension 与 Host 协议不一致 | 实际 background 输出通过 Host parser |
| B-007 | P0 | Windows transport/autostart/collector 未接主路径 | Windows build + runtime 证据 |
| B-008 | P0 | 策略 key 每次 API 启动变化且 Agent 不 pin | API 重启、旧策略、pin/rotation 测试 |
| B-009 | P0 | team scope、headers 审计和 org audit 存在绕过 | 跨组织/跨团队负向测试 |
| B-010 | P1 | enrollment 事务和并发 single-use 不可靠 | 50 并发只有一次成功 |
| B-011 | P1 | health Schema 和健康事实未闭环 | 严格校验 + Web 可见真实状态 |
| B-012 | P1 | delivery-status 与证据冲突 | 状态和证据一一映射 |
| B-013 | P1 | Worker/Web/AI 实际为空 | 真实文件、测试、Runtime |
| B-014 | P1 | Release HTTPS 入口缺失 | 受控网络上的 HTTPS E2E |
| B-015 | P2 | UI 测试复制 HTML，不测真实 main.ts | 导入真实模块的交互测试 |
| B-016 | P2 | Git 跟踪 pycache | 仓库卫生扫描无输出 |

## 5. 审核输出物

Reviewer 在项目内生成：

- docs/reviews/YYYY-MM-DD-integration-ux-audit.md
- docs/reviews/YYYY-MM-DD-findings.json
- docs/evidence/review/YYYY-MM-DD/ 下的合成截图、命令摘要和必要 fixture
- 不修改 docs/evidence/delivery-status.json；只提出建议变更，由实现 Agent 修复并再次审核

审核报告必须包含：

- 审核 branch、HEAD、平台、时间；
- 执行和未执行的命令；
- 每个 Gate 的 PASS / FAIL / BLOCKED；
- Findings，含严重度、证据、影响、复现和验收条件；
- 当前可真实宣称的阶段；
- delivery-status 每个字段的建议值；
- 是否允许进入下一 Wave。

## 6. Gate A：仓库、计划和证据可信度

### A1. 边界与工作区

执行：

    pwd
    git rev-parse --show-toplevel
    git branch --show-current
    git rev-parse HEAD
    git status --short
    git log --oneline --decorate -15

通过标准：

- 根目录准确；
- 审核前工作区状态已记录；
- 没有把用户未提交修改归给实现 Agent；
- 审核输出不覆盖已有文件。

### A2. 生成物和秘密

执行同等扫描：

    git ls-files | rg '(^|/)(target|dist|node_modules|__pycache__)/|\.pyc$|(^|/)\.DS_Store$'
    git grep -n -I -e 'DEEPSEEK_API_KEY' -e 'BEGIN PRIVATE KEY' -e 'device_token'
    git status --ignored --short

人工检查：

- .env 只提交 example；
- fixtures 只含合成值；
- 二进制、数据库、日志和真实截图不入库；
- Agent/扩展产物不含模型 SDK、Prompt、Provider URL 或 key。

任一真实秘密或员工数据为 P0。

### A3. 状态一致性

交叉核对：

- README；
- delivery-status.json；
- phase evidence；
- Git commits；
- 实际代码；
- 本轮命令。

重点：

- pass 是否有日期、平台、命令和结果；
- partial 是否被错误写成 pass；
- Mac 是否替代 Windows；
- debug synthetic 是否替代 release；
- simulated Host 是否替代真实 extension；
- local build 是否替代 package/runtime；
- Mock AI 是否替代 sandbox；
- package 是否替代 release/user installed。

Gate A 通过条件：文档和事实无矛盾。任何 runtime pass 缺真实运行证据为 FAIL。

## 7. Gate B：共享契约与数据边界

### B1. Activity Event

检查：

- JSON Schema additionalProperties=false；
- Rust 和 API 对必填、未知字段、时间、domain、private、url_path 一致；
- state_change 与 focus_segment 互斥；
- event_id / sequence / identity 限制一致；
- API 不信任请求 identity；
- 数据库存储只来自 allowlist。

测试：

- valid fixtures 双语言通过；
- invalid fixtures 双语言拒绝；
- unknown top-level / nested；
- full URL、IP、localhost；
- private event；
- reversed / too long / future / stale；
- analysis 字段注入；
- identity mismatch；
- sequence conflict。

检查数据库 payload，不能只看 rowCount。

### B2. Health

检查：

- API 实际使用 Health Schema；
- unknown 字段、负数、NaN、无效时间、错 device 全拒绝；
- quarantine、last_upload、permissions、autostart、policy 状态可保存；
- Agent 不硬编码 true；
- Operator 可读健康但不可读活动。

### B3. Policy

检查：

- Schema 被 POST /policies 使用；
- private hard drop 不可关闭；
- policy version 原子递增；
- canonicalization 跨语言一致；
- key 持久、pin、rotation、expiry 和 rollback 有测试；
- policy response 检查 credential 过期和吊销；
- API 重启后签名仍有效。

Gate B 通过条件：三类契约在实际路由和 Runtime 中消费，不是只有文件存在。

## 8. Gate C：被监控端 Runtime

Reviewer 必须从 Tauri main 入口追踪：

    main
      → run(background)
      → setup
      → AppRuntime
      → enrollment load
      → SecretStore
      → encrypted LocalStore
      → Engine
      → collectors / browser IPC
      → flush
      → Uploader
      → ACK
      → Health / Policy

逐项确认：

### C1. 启动状态机

场景：

1. 未注册首次启动；
2. 未注册 background 启动；
3. 注册成功不重启；
4. 已注册前台启动；
5. 已注册 background 登录启动；
6. token 缺失；
7. queue key 缺失；
8. config 损坏；
9. crash fuse；
10. 单实例冲突。

通过标准：

- 未注册不采集；
- 首次窗口可见；
- 已注册 background 无窗口；
- token/key 缺失不退回明文；
- safe mode 仍可诊断，且状态真实；
- 状态变化可由 UI 和托盘读取。

### C2. Identity 和持久化

验证：

- config 不含 token；
- SecretStore 是平台实现；
- enroll 后 Engine 立即使用真实 org/device/subject；
- 重启仍使用同一 device；
- revoke/credential error 进入明确状态；
- 配置写入原子；
- 不使用 debug synthetic 身份证明 release。

### C3. Queue 和 sequence

验证：

- 产品路径只能 encrypted；
- 数据库字符串扫描无事件 JSON；
- sequence 跨重启单调；
- reserve 失败不能回退 1；
- queue full 有健康告警；
- flush error 不静默；
- quarantine 也加密或不包含敏感 payload；
- ack 只删精确匹配项。

### C4. 同步循环

验证：

- 采集不被网络阻塞；
- 退避、jitter、429/500/timeout；
- 401/403 停止风暴；
- 网络恢复唤醒；
- policy 定时更新；
- health 周期上报；
- shutdown 可控且不挂住 IPC listener；
- last upload 真实更新。

Gate C 通过条件：有一个进程级自动测试覆盖 enroll → restart → collect → upload → DB → ack，而不是独立 crate 测试拼图。

## 9. Gate D：macOS 与 Windows Collector

### D1. macOS

自动检查：

- NSWorkspace 在主线程；
- app switch；
- idle；
- lock/unlock；
- sleep/wake；
- 15 秒 observation gap；
- 5 分钟 checkpoint；
- 标题默认关闭；
- 敏感 App drop。

真实运行需要单独授权时标 BLOCKED。

不得把 40 秒 debug smoke 写成完整 runtime pass。

### D2. Windows

在 Windows Runner 执行：

    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets --locked -- -D warnings
    cargo test --workspace --locked
    cargo tauri build --debug

源码和 runtime 检查：

- SetWinEventHook 真正由 Runtime 启动；
- message loop 生命周期；
- app_id 是规范化 exe path，不是 PID；
- GetLastInputInfo；
- WTS lock/unlock；
- power suspend/resume；
- Named Pipe 当前用户 ACL；
- DPAPI；
- Task Scheduler XML 真正传入；
- current-user LeastPrivilege；
- normal login 无窗口；
- Edge Native Host registry。

没有 Windows Runner时 Gate D2=BLOCKED，不得 PASS。

## 10. Gate E：浏览器真实链路

### E1. 协议一致性

将扩展真实发送对象序列化，直接交给 Host parser。

验证：

- activate；
- deactivate；
- get_policy；
- policy_snapshot；
- heartbeat；
- error；
- unknown field；
- oversize；
- private mode。

扩展和 Host 必须共享 fixture，不能各测各的。

### E2. 隐私与时间语义

场景：

- focused window active tab；
- background tab；
- window失焦；
- same tab navigation；
- blocked domain；
- non-http；
- localhost / IP；
- incognito；
- service worker sleep/restart；
- Host 断开重连；
- Agent 后启动；
- Chrome 与 Edge browser 字段。

检查：

- 只发送 registrable domain；
- 不发送 path/query/fragment；
- private 永不形成 Observation；
- blocked 页面不会让 current 状态卡死；
- browser domain enrich 同一 segment，不双计 app+site；
- policy title 开关真实生效。

### E3. 平台分发

Mac/Windows 分开检查：

- Host binary 在包中；
- manifest path 指向真实产物；
- Chrome 与 Edge 安装位置/Registry 正确；
- allowed_origin 与正式 ID 一致；
- install/uninstall 对称；
- 未授权不实际安装。

真实浏览器未加载时只能写 component_verified，不能写 runtime_verified。

## 11. Gate F：API、数据库、认证和 Scope

### F1. Enrollment 并发与事务

测试：

- 正常 201；
- 缺失、无效、过期、已用；
- 50 并发同 code；
- DB 中只产生一个 credential/device；
- 任一中间 SQL 失败全部 rollback；
- device_label 保存；
- token 只返回一次；
- token hash 入库；
- 过期和 revoke。

检查实现是否使用专用 client，而不是 pool.query 包装 BEGIN/COMMIT。

### F2. Admin Session

检查：

- Argon2id；
- login rate limit；
- idle / absolute timeout；
- logout；
- cookie/BFF 语义；
- localStorage 无 token；
- session cleanup；
- request_id；
- 401 与 403 区分。

### F3. Scope 负向矩阵

为两个 org、两个 team、四个 role 建合成矩阵。

测试：

- Manager 只看授权 team；
- Admin 只看本 org；
- Auditor 只看审计范围；
- Operator 只看 health；
- 跨 org subject ID；
- 跨 org device；
- audit logs org filter；
- activity/headers 无旁路；
- 个人访问均有 audit；
- 策略/导出/删除权限。

任一跨 Scope 成功为 P0。

### F4. 数据库

检查：

- migration ledger；
- transactional migration；
- FK 与复合唯一；
- cursor pagination；
- time/org indexes；
- no raw input JSON；
- title 默认不查；
- health latest query；
- idempotency；
- retention jobs 后续 Gate。

## 12. Gate G：Worker 与确定性分析

没有 Worker 源码时直接 FAIL，不因 API 有数据而跳过。

### G1. 分类

验证：

- app/domain rules；
- unknown；
- rule version；
- admin correction；
- deterministic golden fixture；
- 重跑一致。

### G2. 聚合

Golden dataset 必须覆盖：

- 跨午夜；
- 时区；
- 重叠；
- duplicate；
- idle/lock/sleep；
- offline gap；
- browser enrichment；
- late arrival；
- rerun；
- team 人数小于 5。

断言总时长守恒，误差有上限。

### G3. 运维

检查：

- watermark；
- retry/dead letter；
- heartbeat；
- lag；
- job idempotency；
- API ingest 不被 Worker 故障影响；
- System API 可读状态。

Gate G 通过条件：数据库中生成可追溯 metric_id 和 calculation_version。

## 13. Gate H：Web Console 前后端接入

apps/web-console 无实现时直接 FAIL。

### H1. BFF 与认证

检查：

- Web 不直连 DB；
- Browser 不持久化 Bearer token；
- BFF 只转发允许 DTO；
- 401 跳登录；
- 403 显示无权限；
- CSRF/session cookie；
- request_id 可见；
- 不在客户端 bundle 泄漏 server URL、secret 或内部成本。

### H2. 页面与 API 矩阵

| 页面 | 必验数据 | 必验异常 |
| --- | --- | --- |
| 登录 | session/me | 错密码、限速、过期 |
| 总览 | coverage、online、delay、category | 空、过期、部分失败 |
| 团队 | summary、trend、sample size | 少于 5 人 |
| 人员 | timeline、gaps、audit | 无权限、离线不是无活动 |
| 设备 | latest health、queue、version | stale、revoked、permission |
| Enrollment | code、expiry、status | 已用、过期、重复提交 |
| 策略 | versions、diff、rollout | invalid、conflict、rollback |
| 审计 | cursor、filters | 跨 org |
| Insight | evidence、coverage、feedback | provider fail、证据不足 |
| 系统 | API/Worker/DB/Provider | degraded |

每个网络请求要能在服务端 log 和 audit 中用 request_id 对上。

### H3. UX 人工审核

端侧和监控端分别检查：

- 信息层级；
- 主任务是否 1–2 步可达；
- 文案是否描述用户结果而非内部实现；
- 不把错误成功化；
- 数据 freshness；
- no activity 与 missing data；
- destructive action 确认；
- loading/empty/error/partial；
- keyboard/focus；
- 200% zoom；
- 360/768/1024/1440；
- dark/light；
- reduced motion；
- 图表的文字/表格替代；
- 中文长文本；
- 表格分页和大数据性能。

禁止用任意“漂亮度分数”替代具体 PASS/FAIL 条件。

### H4. 端侧 UX 专项

首次配置必须实际完成：

1. 连接检测；
2. enrollment；
3. 权限状态；
4. 浏览器连接；
5. 自启动/首个健康确认。

审核错误体验：

- 无网络；
- TLS 错；
- code 无效、已用、过期；
- SecretStore 失败；
- autostart requires approval；
- Browser Host 缺失；
- upload 超时。

完成页必须逐项显示事实，不能一句“已后台运行”覆盖失败。

## 14. Gate I：DeepSeek 监控端边界

无 AI 实现时直接 FAIL；Fake 只能通过 local_tests。

### I1. 产物边界

扫描 Agent、Extension、Host：

- 无 Provider SDK；
- 无 Prompt；
- 无 model name/config；
- 无 API key；
- 无 Insight Schema 运行逻辑；
- 无分类/报告。

### I2. 输入最小化

拦截 Fake/DeepSeek Adapter 请求，断言只含：

- aggregate metrics；
- metric IDs；
- coverage/gaps；
- date range；
- rule facts。

发现 raw title、完整 event、URL、token 为 P0。

### I3. 输出校验

测试：

- valid JSON；
- malformed；
- hallucinated metric ID；
- wrong number；
- no evidence；
- timeout；
- 429；
- provider down；
- prompt injection-like text in category labels；
- oversized response。

基础统计必须在所有 AI 失败场景仍可用。

### I4. Sandbox

只有真实 sandbox 调用、使用合成聚合并记录 provider response validation，才能建议 deepseek_sandbox_verified=pass。未授权或未配置时为 BLOCKED/UNVERIFIED。

## 15. Gate J：可靠性、性能和长时间运行

### J1. 故障注入

- API 关闭 30 分钟；
- DNS/TLS fail；
- PostgreSQL restart；
- Worker crash；
- disk full / queue full fixture；
- corrupt config；
- wrong queue key；
- Agent kill -9；
- clock jump；
- duplicate process；
- policy expiry；
- credential revoke。

检查无静默丢失、无紧密重试循环、状态可诊断。

### J2. 8 小时门禁

每个平台分别记录：

- RSS；
- CPU P95；
- queue depth；
- upload latency；
- network bytes；
- capture rate；
- duration error；
- crashes；
- gaps；
- battery impact；
- first/last sample；
- 工具版本和设备信息。

样本和汇总必须保存进项目 evidence；只写“执行中”不能 pass。Mac 不能替代 Windows。

### J3. 7 天门禁

Pilot 前至少验证：

- memory 无持续增长；
- online rate；
- autostart；
- API/Worker lag；
- retention；
- policy rollout；
- update failure recovery。

未达到时不宣称 Pilot ready。

## 16. Gate K：构建、打包、更新和分发证据

逐项独立审核：

### K1. Build

- Mac Tauri build；
- Windows Tauri build；
- Browser bundle；
- API build；
- Worker build；
- Web build；
- Monitor container images。

cargo workspace build 不自动等于 Tauri app build。

### K2. Package

- .app/.dmg 或对应 Mac 包；
- Windows installer；
- Extension zip；
- checksums；
- SBOM；
- artifact boundary scan；
- clean machine unpack validation。

### K3. Runtime

- installed app；
- 首次配置；
- 登录后台；
- actual browser；
- monitor stack；
- update；
- rollback；
- uninstall。

### K4. External delivery

以下不能由本地证据推导：

- Apple 签名；
- notarization；
- Windows code signing；
- production updater；
- managed browser distribution；
- remote release；
- production update；
- Pilot deployed；
- user installed。

没有对应外部事实一律 unverified 或 blocked。

## 17. 建议验证命令矩阵

Reviewer 应根据当前实现补全命令，但最低包含：

### Rust

    cd apps/endpoint-agent/src-tauri
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets --locked -- -D warnings
    cargo test --workspace --locked
    cargo build --workspace --locked

### Endpoint UI

    cd apps/endpoint-agent/src-ui
    npm run typecheck
    npm test
    npm run build

### Browser Extension

    cd apps/browser-extension
    npm test
    npm run build

### API

    cd apps/api
    npm run typecheck
    npm test

### Top-level E2E

必须有项目统一命令。若没有，这是 Finding。它需要自动启动隔离服务，而不是要求 Reviewer 手工准备遗留进程。

### Worker/Web

实现后至少：

- typecheck；
- unit；
- integration；
- production build；
- Playwright；
- accessibility；
- server/client boundary scan。

### Release Verifier

    python3 -m unittest discover -s tools/release-verifier -p 'test_*.py'

Verifier 必须针对真实 release/bundle 产物执行，不能只测 verifier 自己。

## 18. UX 验收用例

### 18.1 IT 首次部署

前置：未注册合成设备。

成功路径：

- 打开 App；
- 连接监控端；
- 输入 code；
- 完成权限；
- 确认浏览器；
- 确认后台状态；
- 关闭窗口；
- Agent 持续运行；
- Monitor 设备页 30 秒内出现真实 health。

失败路径逐一验证并截图。

### 18.2 日常后台

- 登录启动不弹窗；
- 托盘状态正确；
- 离线显示排队；
- 网络恢复更新 last upload；
- 状态页不显示 token；
- 退出行为和影响说明清楚；
- 设置窗口关闭后采集持续。

### 18.3 Manager 查看团队

- 只看到授权 team；
- 覆盖率优先；
- 少于 5 人不显示匿名对比；
- 无个人排名；
- 缺数不形成效率结论。

### 18.4 Admin 查看个人

- 打开前明确权限和审计；
- audit row 与 request_id 对应；
- timeline 与聚合守恒；
- offline gap 区分；
- 标题默认隐藏；
- 跨 org URL 直接请求被 API 拒绝。

### 18.5 Operator 诊断设备

- 看得到 health、version、queue、permission、autostart；
- 看不到 app/domain/timeline/Insight；
- 异常可筛选；
- stale 状态显著。

### 18.6 Admin 查看 AI

- 每条 finding 有 evidence；
- 点击能到确定性指标；
- 数据缺失显示 limitation；
- Provider fail 时基础页面正常；
- 反馈可提交并审计。

## 19. 审核判定规则

### 19.1 Wave PASS

同时满足：

- 本 Wave 所有 P0/P1 已关闭；
- 自动测试新鲜通过；
- 关键真实链路有进程级证据；
- 文档状态准确；
- 未授权外部项明确 blocked；
- 没有依赖下一 Wave 掩盖当前缺口。

### 19.2 条件 PASS

只允许存在不影响当前 Wave 核心目标的 P2/P3，必须有 owner、验收条件和后续 Task。P0/P1 不允许条件通过。

### 19.3 FAIL

任一情况：

- 核心链路断开；
- Scope 绕过；
- 明文敏感数据；
- 静默丢事件；
- UI 虚假成功；
- 组件测试冒充 E2E；
- evidence 虚报；
- Mac/Mock/debug 冒充另一证据层。

### 19.4 BLOCKED

仅用于确实需要用户授权、Windows 真机、签名凭据、受管浏览器或真实 Provider 的步骤。Blocked 不是 Pass，也不自动否定已经通过的本地组件。

## 20. delivery-status 建议审核表

Reviewer 在报告中逐项给建议，不直接编辑原文件：

| 字段 | 通过所需最低证据 |
| --- | --- |
| local_review | 本独立审核无 P0/P1 |
| local_tests | 标准矩阵全部新鲜通过 |
| local_build_mac | Mac 产品入口 build |
| local_build_win | Windows 原生 build |
| local_build_monitor | API/Worker/Web/gateway images build |
| local_package_mac | 可检查 Mac 安装包 |
| local_package_win | 可检查 Windows installer |
| local_package_browser | Chrome/Edge bundle |
| runtime_verified_mac | Release Runtime 全链路，不是 debug short smoke |
| runtime_verified_win | Windows Release Runtime 全链路 |
| runtime_verified_monitor | Monitor stack 实际启动和页面/API/Worker |
| deepseek_sandbox_verified | 合成数据真实 Provider sandbox |
| local_update_verified | 本地签名 update/rollback |
| signed_package_mac | 有效 Developer ID 验证 |
| notarized_package_mac | Apple notarization |
| signed_package_win | 有效 Windows code signature |
| browser_distribution_verified | 受管 Chrome/Edge 实际策略 |
| remote_release | 真实远端 release |
| update_verified | 真实发布渠道更新 |
| pilot_deployed | 经授权 Pilot 实际部署 |
| user_installed | 用户设备实际安装证据 |

当前基线建议：

- local_tests：pass，可以保留，但只表示现有矩阵；
- local_build_mac：需要说明是 workspace build 还是 Tauri product build；
- runtime_verified_mac：应为 unverified；
- 其余现有 unverified 保持；
- local_review：本次发现多个 P0，应改为 fail 或 unverified，取决于状态模型的使用规则。

## 21. Finding 模板

每个 Finding 使用：

### [P0] 标题

- ID：
- 组件：
- 发现版本：
- 证据路径/行号：
- 复现命令：
- 实际结果：
- 期望结果：
- 用户/数据影响：
- 为什么现有测试没发现：
- 修复验收条件：
- 需要回归的 Gate：
- 状态：Open / Fixed pending review / Closed / Blocked

不要只写“建议优化”；必须让实现 Agent 能复现和让 Reviewer 能关闭。

## 22. 最终审核报告模板

**报告标题：WorkInsight 独立审核报告**

### 审核信息

- Branch：
- Commit：
- Platform：
- Date：
- Reviewer：
- Worktree status：

### 总结

- Verdict：PASS / CONDITIONAL PASS / FAIL / BLOCKED
- 可真实宣称的阶段：
- 是否允许进入下一 Wave：
- P0 / P1 / P2 / P3 数量：

### Gate 结果

| Gate | 结果 | 核心证据 | 阻断 |
| --- | --- | --- | --- |
| A 仓库/证据 | | | |
| B 契约 | | | |
| C Runtime | | | |
| D 平台 | | | |
| E Browser | | | |
| F API/DB/RBAC | | | |
| G Worker | | | |
| H Web/UX | | | |
| I AI | | | |
| J 可靠性 | | | |
| K 交付 | | | |

### Findings

按 P0 → P3 排序。

### 新鲜验证

列命令、通过数量、失败和未执行原因。

### UX 结果

- Endpoint first-run：
- Endpoint background：
- Monitor overview：
- Team/subject：
- Device：
- Policy/audit：
- Insight：
- Accessibility/responsive：

### 前后端接入结果

逐链路写 Connected / Partial / Disconnected / Not implemented。

### delivery-status 建议

逐字段列建议值和证据。

### 未授权或外部 Blocker

只列真实 blocker，不用它掩盖本地可修问题。

### 下一步

每个 Open Finding 映射到实施计划 Task。

## 23. 可直接交给审核 Agent 的首条指令

你是独立 Reviewer，不是实现者。只允许在“电脑监控软件”目录内工作。先记录 branch、HEAD 和工作区，再按本审核计划从 Gate A 开始。不要相信提交信息、README 或 delivery-status，必须从产品入口追踪真实数据流并运行新鲜验证。发现问题只写审核报告和 findings，不修改源代码，不替实现 Agent 修。组件单测不能替代进程级 E2E；Mac 不能替代 Windows；debug/Mock/模拟扩展不能替代 Release、真实 Provider 或真实浏览器。任何安装、登录项、Native Host、注册表、注销、系统目录写入、付费 Provider、push 或发布动作都必须先获得用户当次明确授权，未授权写 BLOCKED。最终给出 PASS / FAIL / BLOCKED、是否允许进入下一 Wave，以及 delivery-status 每个字段的建议值。
