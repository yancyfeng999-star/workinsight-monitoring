# 安全政策

WorkInsight 处理终端活动元数据、设备身份、策略和上传队列。请把任何可能导致数据泄露、权限绕过、策略绕过、凭据暴露或远程执行的问题当作安全问题处理。

## 报告方式

请优先通过 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 创建私密报告。如果该入口不可用，请通过仓库维护者 `yancyfeng999-star` 的 GitHub 账号私下联系，不要在公开 Issue、Pull Request 或讨论区粘贴漏洞细节。

报告至少应包含：

- 受影响的提交、版本或组件路径；
- 可复现的最小步骤或安全的概念验证；
- 影响范围、前置条件和可能的缓解措施；
- 你是否在测试环境中使用了真实个人活动数据。

请使用合成数据、脱敏日志和临时凭据。不要提交 API 密钥、模型密钥、私钥、Cookie、真实 URL、窗口标题、聊天/邮件正文、员工活动记录或生产数据库内容。

## 响应流程

维护者会先确认收到报告，再复现、评估影响、制定修复和公开时间。公开披露前会尽量协调修复版本、缓解措施和致谢方式；无法复现或不属于安全问题的报告，也会说明处理结论。

本文件不承诺固定响应时限，也不替代部署方的事件响应制度。自托管使用者仍需配置最小权限、密钥轮换、日志保留、数据库备份和访问审计。

## 当前安全边界

- Agent 默认不采集截图、录屏、键盘、剪贴板、网页正文、表单、Cookie 或聊天/邮件正文。
- `private_mode=true` 事件应在 Agent 层拒绝入队；完整 URL path/query/fragment 默认禁止。
- 模型密钥、Prompt 和 Provider 配置不得下发到 Agent 或浏览器扩展。
- 不得通过伪装进程、绕过 TCC/UAC、远程 Shell 或任意脚本扩大权限。

如果发现文档声明与实际代码不一致，请按安全报告流程处理，并在修复前不要把未验证能力当作生产承诺。
