# WorkInsight 后续实施计划：前后端真实接入与用户体验闭环 v2.0

> 日期：2026-08-12
> 审查基线：分支 fix/wave0-1，提交 5021f94
> 适用目录：本项目根目录及其子目录
> 目标读者：后续实现 Agent、负责验收的 Reviewer
> 状态：待执行，不代表任何任务已经完成

## 0. 先说结论

当前仓库已经从“局部原型”推进到“多数组件有代码、有单元测试”的阶段，但还不是可用的监控系统。

严格按真实数据链路判断：

- Task 1–2 的仓库基线和事件契约大体形成，但仍有生成物入库、契约未被所有运行路径实际消费等问题。
- Task 3–8 都有一定实现量，但没有任何一个达到原计划的完整 Exit Gate。
- 被监控端的核心真实链路没有闭环：首次注册、重启恢复、加密队列、策略、上传、健康、后台 UI 目前是分散组件，主程序没有把它们组成一个可持续运行的 Runtime。
- 浏览器扩展与 Native Host 的消息协议不一致，现有“端到端”证据只模拟了 Host 输入，并没有让真实扩展通过。
- Windows 模块存在，但没有接入主循环；Native Host 和自启动实现也存在平台阻断，不能称为 Windows 路径完成。
- 监控端 API 已有注册、上传、部分 RBAC 和策略路由，但还不能安全、完整地支撑管理前端。
- apps/worker 和 apps/web-console 没有实现文件；分类、聚合、数据质量、Web Console、DeepSeek 分析均未开始。
- 当前 delivery-status 将 runtime_verified_mac 标成 pass，与证据文档中的“部分验证”及实际接线状态矛盾，必须先恢复事实状态。

因此，下一步不能直接从旧计划 Task 9 开始做 Web Console。必须先关闭端侧和 API 的真实接线缺口，再建立监控端 Worker 和前端。

## 1. 本次审查范围与证据

### 1.1 审查过的内容

- Git 分支、提交历史、工作区状态和已跟踪生成物；
- README、产品合同、决策登记、交付状态和 Phase 1 证据；
- endpoint-agent 的 Tauri 入口、Engine、SecretStore、LocalStore、Uploader、Policy Client、Autostart、macOS/Windows Collector；
- 浏览器扩展、Native Messaging Host、same-user IPC；
- API 路由、认证、RBAC、数据库迁移和集成测试；
- Worker、Web Console、CI、打包与发布目录的实际文件状态；
- 端侧首次配置界面的结构、状态反馈、可达性和错误体验；
- 当前前端到后端的接口是否真实可调用。

### 1.2 2026-08-12 新鲜验证结果

以下命令在当前 Mac、本地测试 PostgreSQL 上重新执行并通过：

| 范围 | 验证 | 结果 |
| --- | --- | --- |
| Rust Agent | fmt、clippy、workspace test、workspace build | 通过；69 项测试 |
| API | TypeScript typecheck、npm test | 通过；22 项测试 |
| Endpoint UI | typecheck、Vitest、Vite build | 通过；3 项测试 |
| Browser Extension | Node test、bundle build | 通过；19 项测试 |
| Release Verifier | Python unittest | 通过；5 项测试 |
| Impeccable 静态 UI 扫描 | endpoint src-ui | 0 条模板规则命中 |

这些结果只证明本机静态质量和现有测试通过，不证明：

- Tauri 首次配置窗口实际出现；
- enrollment 后 Engine 得到真实身份；
- token 在重启后仍可读取；
- 生产运行使用加密队列；
- Agent 调用 Uploader、Policy Client 和健康上报；
- 真实 Chrome/Edge 扩展消息能通过 Native Host；
- Windows 能编译、安装、自启动和运行；
- 监控端 Web、Worker、AI 存在；
- 本地包、签名包、远端发布、更新、Pilot 或用户安装完成。

## 2. 当前进度审查

### 2.1 按旧计划任务判断

| 旧任务 | 当前事实 | 严格状态 | 后续动作 |
| --- | --- | --- | --- |
| Task 1 仓库基线 | 标准命令可跑，target 已移出索引；仍跟踪 pycache 文件，README 状态过时 | 部分通过 | Gate 0 纠正 |
| Task 2 事件契约 | JSON Schema、Rust/TS 校验和 fixtures 存在 | 基本形成 | 将 Schema 接入健康、策略和真正 E2E |
| Task 3 注册/认证/RBAC | 路由和集成测试存在 | 部分实现 | 修事务、并发、Scope、审计、策略密钥 |
| Task 4 单一 Engine | Engine 单测与短时 Mac smoke 存在 | 组件通过、Runtime 未闭环 | 主程序加载真实身份并接入状态事件 |
| Task 5 Secret/加密/自启动/UI | 库和少量平台代码存在 | 未闭环 | 平台存储、加密队列、窗口、自启动真实接入 |
| Task 6 浏览器链路 | 扩展、Host、IPC 各自存在 | 真实链路失败 | 统一协议、平台传输、真实浏览器验证 |
| Task 7 上传/策略/健康 | Uploader 与验签单测存在 | 未接入主程序 | 建立后台同步循环和完整健康事实 |
| Task 8 Windows | Windows Collector 模块存在 | 未集成、未原生验证 | 修 Windows 编译、事件源、自启动和 Named Pipe |
| Task 9 Worker | 目录为空 | 未开始 | 完成前述 P0 后实施 |
| Task 10 Web Console | 目录为空 | 未开始 | 在 Read API 稳定后实施 |
| Task 11–12 DeepSeek | 无 Worker/Schema/UI | 未开始 | 确定性聚合完成后实施 |
| Task 13–16 生命周期/交付 | 主要是文档和 verifier | 未开始或局部准备 | 后置执行 |

### 2.2 当前前后端接入矩阵

| 链路 | 代码是否存在 | 是否在主运行路径 | 是否有真实 E2E | 结论 |
| --- | --- | --- | --- | --- |
| Endpoint 注册页 → Tauri enroll | UI 和命令存在 | Tauri 没有创建窗口 | 否 | 不可用 |
| enroll → API /v1/enroll | 两端代码存在 | 命令可发请求 | 无真实 UI 测试 | 局部可用 |
| enroll 身份 → Engine | 配置会写文件 | Engine 不加载配置，也无运行时通知 | 否 | 断开 |
| device token → 重启恢复 | SecretStore 接口存在 | 主程序使用 MemorySecretStore | 否 | 断开 |
| Agent 事件 → 加密 SQLite | 加密实现和单测存在 | 主循环调用明文 open | 否 | 断开 |
| Engine → Uploader → API | Uploader 组件存在 | collector_loop 不调用 Uploader | 否 | 断开 |
| Agent → Policy API | API 和验签函数存在 | 无获取、固定公钥、缓存或应用逻辑 | 否 | 断开 |
| Agent → Health API | 两端代码存在 | token 获取固定返回 None | 否 | 断开 |
| Extension → Native Host | 两端代码存在 | 消息类型和字段不一致 | 否 | 断开 |
| Native Host → Agent IPC | Mac Unix Socket 路径存在 | 模拟 Host 输入可通 | 部分 | 仅组件链路 |
| Windows Host → Agent | manifest 和脚本存在 | Host 使用 UnixStream | 否 | 平台不成立 |
| API → PostgreSQL | 路由和迁移存在 | 本地集成测试通过 | 是，API 级 | 部分可用 |
| API → Worker | 无 Worker | 否 | 否 | 未开始 |
| API → Web Console | 无 Web Console | 否 | 否 | 未开始 |
| Worker → DeepSeek | 无实现 | 否 | 否 | 未开始 |

### 2.3 P0：继续开发前必须处理的阻断

#### P0-01 Release Engine 永远拿不到真实 enrollment 身份

主程序只在 debug 模式注入合成身份。Release 启动时没有从 config.json 恢复 org/device/subject；enroll 命令也不会把新身份通知正在运行的 Engine。结果是 release Engine 会持续丢弃所有 Observation。

#### P0-02 上传和策略未进入主循环

Uploader、Policy Client 只是独立 crate。collector_loop 只采集和 flush，不调用上传、不拉策略、不维护退避、不处理凭据过期。

#### P0-03 生产路径仍使用明文队列

LocalStore::open_encrypted 有实现，但两个实际 Agent 入口都调用 LocalStore::open，后者是 plain 模式。现有“加密队列通过”只能说明库单测通过。

#### P0-04 持久凭据未使用

Tauri AppState 实际创建 MemorySecretStore；secret_token 固定返回 None；Windows DPAPI Store 仍是 pending stub。token 重启即丢，健康和上传均不能工作。

#### P0-05 首次配置 UI 不可达

tauri.conf.json 的 windows 为空，setup 代码没有动态创建窗口。background 标志只写入 AtomicBool，未控制窗口；托盘也没有“设置/诊断”入口。当前 UI 构建成功但用户看不到。

#### P0-06 浏览器线上协议不一致

扩展发送 browser_active / browser_inactive，并携带 browser 字段；Native Host 解析 activate / deactivate，并要求 private_mode。当前真实扩展消息会被 Host 判为 invalid message。现有证据通过的是模拟 Host 所需格式，不是扩展实际格式。

#### P0-07 Windows 路径不是可运行实现

- 主循环仍轮询 current_foreground，没有启动 SetWinEventHook；
- idle、锁屏、睡眠/唤醒模块没有发 Observation 给 Engine；
- browser-bridge 无条件使用 std::os::unix::net::UnixStream；
- Task Scheduler 代码生成 XML，却没有把 XML 写入 schtasks 的 stdin；
- Windows DPAPI 未实现；
- 没有 Windows 编译或真机证据。

#### P0-08 策略信任链不成立

- API 默认每次启动生成新的 Ed25519 key；
- 已存策略可能由旧 key 签名，但响应返回当前新 public key；
- enrollment 响应中的 public key 没有被 Agent 保存和 pin；
- policy-client 没有 HTTP 获取、canonicalization、缓存和应用逻辑；
- policies.ts 在 ESM 中使用 require，命中路由时有运行时失败风险；
- 新策略请求没有执行共享 Schema 校验。

#### P0-09 RBAC 和审计仍有绕过路径

- Manager 可按 org 查询任意 subject，没有 team scope；
- activity/headers 与个人明细返回相同内容，却不写访问审计；
- audit_logs 没有 org_id，查询没有组织过滤；
- subject upsert 的全局主键冲突可能让一个组织修改另一个组织的显示名；
- 策略和健康查询没有完整的 credential 过期、Scope 与 Schema 处理。

### 2.4 P1：会制造错误完成感的问题

- enrollment 使用 pool.query 执行 BEGIN/COMMIT，不能保证所有语句落在同一连接；并发请求也可能重复消费同一个 code。
- activity_segments.payload 仍保存完整 JSON，和旧计划“allowlist 字段、禁止原对象直存”相反。
- health API 没有使用 health Schema，也不保存 quarantine、last upload、policy error 等关键字段。
- HealthContext 把 permissions_ok 和 autostart_enabled 硬编码为 true。
- Agent 的 reserve_sequence 失败时回退到 1，flush 忽略入队错误，可能静默丢失或制造 sequence 冲突。
- API 默认只绑定 127.0.0.1 且只提供 HTTP；Release Agent 又只允许 HTTPS，监控端尚无可达 TLS 入口。
- UI 测试复制了一份 HTML 片段，没有导入 main.ts，不能发现真实页面被删、命令参数错误或窗口不可达。
- Extension 测试多为静态字符串契约，没有让真实 background.js 对接真实 Host parser。
- tests/e2e 直接用 fetch 上传合成事件，没有启动 Rust Agent；也未接入标准测试命令。
- delivery-status 的 runtime_verified_mac=pass 与证据文档“部分”矛盾。
- README 同时把已经存在的组件写成“未完成”，又没有列出真正断链，状态信息已过时。

### 2.5 关键源码证据定位

| 结论 | 当前证据位置 |
| --- | --- |
| 主循环只给 debug 合成身份，Release 不恢复 enrollment | apps/endpoint-agent/src-tauri/src/lib.rs:159–175 |
| 主循环使用 plain LocalStore | apps/endpoint-agent/src-tauri/src/lib.rs:159；crates/agent-bin/src/main.rs:66–68 |
| Tauri 实际使用 MemorySecretStore | apps/endpoint-agent/src-tauri/src/lib.rs:50–77 |
| 健康 token 获取固定返回 None | apps/endpoint-agent/src-tauri/src/lib.rs:238–297 |
| 主循环没有 Uploader 和 Policy 调用 | apps/endpoint-agent/src-tauri/src/lib.rs:144–236 |
| Tauri 没有声明或动态创建窗口 | apps/endpoint-agent/src-tauri/tauri.conf.json:12–16；src/lib.rs:50–104 |
| 扩展发送 browser_active/browser_inactive | apps/browser-extension/src/core/background.js:77–106 |
| Host 只解析 activate/deactivate/heartbeat | apps/endpoint-agent/src-tauri/crates/browser-bridge/src/protocol.rs:3–16 |
| Windows Host 仍使用 UnixStream | apps/endpoint-agent/src-tauri/crates/browser-bridge/src/main.rs:95–109 |
| Windows Task XML 未写入 schtasks stdin | apps/endpoint-agent/src-tauri/crates/autostart-supervisor/src/windows.rs:17–62 |
| API 启动时临时生成策略 key | apps/api/src/index.ts:21–31 |
| 策略 ESM 路径使用 require | apps/api/src/routes/policies.ts:109–112 |
| enrollment 事务使用 pool.query | apps/api/src/routes/enrollment.ts:24–57 |
| headers 查询绕开个人访问审计 | apps/api/src/routes/subjects.ts:32–47 |
| 审计日志查询没有 org 条件 | apps/api/src/routes/admin.ts:44–50 |
| runtime 状态与部分证据矛盾 | docs/evidence/delivery-status.json:10；docs/evidence/phase-1/mac-collector-smoke.md:33–38 |
| Worker 与 Web Console 无实现文件 | apps/worker；apps/web-console |

## 3. 目标产品体验

### 3.1 被监控端体验原则

被监控端不是日常工作应用。它只应有两个可见时刻：

1. IT/管理员首次安装配置；
2. 需要查看状态或诊断时，从菜单栏/托盘主动打开。

正常登录后：

- 默认后台启动；
- 不弹主窗口；
- 不抢焦点；
- 不显示持续通知；
- 托盘只显示真实健康状态；
- 采集、排队、上传和策略失败必须可诊断，不能伪装成“运行正常”。

#### 3.1.1 首次配置流程

建议使用 5 步清晰流程，而不是一个一次提交的表单：

| 步骤 | 用户任务 | 必须显示的真实状态 |
| --- | --- | --- |
| 1. 连接监控端 | 输入或粘贴监控端地址 | DNS/TLS/健康检查结果 |
| 2. 绑定设备 | 输入一次性注册码和设备名 | 组织、员工、设备 ID 的安全摘要 |
| 3. 系统权限 | 检测平台所需权限 | 已授权、待授权、不可用、重试 |
| 4. 浏览器接入 | 检查 Chrome/Edge Host 与扩展 | 未安装、已安装未连接、已连接 |
| 5. 后台运行 | 注册登录项并发首个健康样本 | enabled、requires approval、failed、last upload |

完成按钮只有在真实 enrollment 成功后出现。最终页不得只写“Agent 已后台运行”，而应分别显示：

- 注册成功；
- 数据队列可写且加密；
- 登录启动项状态；
- 监控端连接状态；
- 首个健康样本是否确认；
- 浏览器扩展是否已连接；
- 需要用户继续处理的系统批准。

#### 3.1.2 状态页与托盘

托盘菜单建议：

- 状态：正常 / 等待授权 / 离线排队 / 需要处理；
- 打开状态与诊断；
- 复制诊断编号；
- 关于；
- 退出。

状态页只显示运维信息：

- 设备名称、版本、设备 ID 后 6 位；
- 监控端地址的主机名，不显示 token；
- 最近采集、最近上传、队列数量、隔离数量；
- 自启动和系统权限；
- Chrome/Edge 扩展连接；
- 当前策略版本和最后更新时间；
- 重试按钮与安全的诊断导出。

不提供员工暂停采集入口；也不显示个人行为明细或 AI 分析。

#### 3.1.3 表单和错误体验

必须补齐：

- 提交时禁用按钮，防止重复 enrollment；
- URL 先做规范化和连接测试；
- 注册码关闭自动大写、拼写检查，支持粘贴；
- 错误按可行动类别显示：地址不可达、TLS 不可信、注册码无效/已使用/过期、服务器错误；
- 默认只显示友好文案和诊断 ID，原始响应放在可复制的诊断信息中；
- 键盘焦点、可见 focus ring、ARIA live、200% 缩放和深色模式；
- 成功后不让用户误以为所有平台项都已通过。

### 3.2 监控端 Web Console 信息架构

监控端是本产品的主界面。建议桌面优先，同时支持 1024px 宽度。

#### 全局导航

1. 总览
2. 团队与人员
3. 设备
4. 分析报告
5. 采集策略
6. 审计与导出
7. 系统状态

导航必须按角色隐藏无权入口，但 API 仍是最终权限边界。

#### 3.2.1 总览

首屏不做“员工排名”，而是回答：

- 今天有多少设备在线；
- 数据覆盖率是否足够；
- 工作时间主要分布在哪些类别；
- 相比上一工作日/周发生了什么变化；
- 哪些异常来自真实行为，哪些只是离线、权限或缺数；
- 最新日/周 Insight 是否可用。

建议结构：

- 数据新鲜度横幅；
- 在线设备、有效覆盖率、上传延迟、待处理设备四个核心指标；
- 类别时间分布；
- 团队趋势；
- 数据缺口和运维异常；
- 最新 AI 摘要，附可核对指标链接。

#### 3.2.2 团队与人员

团队页：

- 仅在人数不少于 5 时显示匿名汇总；
- 显示分布和趋势，不显示个人排行榜；
- 明确样本人数、覆盖率和缺失比例；
- 支持日期、团队、类别筛选。

人员页：

- 需要权限并在打开前说明会写审计；
- 时间线按应用/网站类别合并；
- 浏览器只显示 registrable domain；
- “无活动”与“设备离线/权限缺失/未上传”必须视觉区分；
- 标题默认不展示；有权且策略允许时按需展开并记录审计；
- 所有统计可回溯到时间段，而不是只给一个评分。

#### 3.2.3 设备

设备页应成为运维闭环：

- 在线、延迟、离线、已吊销、待批准；
- OS、Agent 版本、最后健康、最后上传、队列、隔离、权限、自启动、策略版本；
- 按异常优先排序；
- 设备详情提供诊断时间线；
- 生成注册码、吊销、重试策略、版本分布；
- 注册码只显示一次，明确有效期和绑定员工。

#### 3.2.4 策略

策略编辑器需要：

- 当前生效版本和生效设备比例；
- 字段级说明和安全默认值；
- 窗口标题默认关闭；
- 不可删除的本地硬规则单独显示；
- 变更前 diff 预览；
- 发布后回滚到旧版本，但不原地修改旧版本；
- 无效域名、过期时间和空规则即时校验；
- 每次变更填写原因并进入审计。

#### 3.2.5 AI 分析

AI 不应成为无证据的聊天框。每条结论包括：

- 结论；
- 使用的数据周期；
- evidence metric ID；
- 可展开的确定性数字；
- 数据覆盖率和缺口；
- 置信级别或“证据不足”；
- 有帮助 / 事实错误反馈；
- 模型失败时保留确定性统计，不显示伪造摘要。

#### 3.2.6 全局空、错、加载状态

每个页面至少有：

- 首次使用空状态；
- 筛选后无结果；
- 无权限；
- 数据仍在聚合；
- API 不可达；
- 数据过期；
- 部分数据缺失；
- 请求超时和安全重试；
- Skeleton 不遮蔽关键信息；
- 错误页带 request ID，不泄漏内部堆栈。

## 4. 目标数据流

    macOS / Windows OS Events
              |
              v
        Platform Collectors
              |
    Chrome / Edge Extension
              |
        Native Messaging
              |
              v
         Agent Runtime
      - Enrollment Identity
      - Local hard filters
      - Segment Engine
      - Encrypted Queue
      - Policy cache
      - Uploader / Health
              |
          HTTPS Gateway
              |
              v
          Monitor API
      - Device authentication
      - Admin session / RBAC
      - Strict schemas
      - Idempotent ingest
              |
              v
          PostgreSQL
       raw segments / health
              |
              v
             Worker
      classify / aggregate / rules
              |
         +----+----+
         |         |
         v         v
    Read-model API  DeepSeek adapter
         |         |
         +----+----+
              |
              v
          Web Console

硬边界：

- Agent、扩展和 Host 不包含分类、统计、Prompt、模型 SDK、Provider 密钥；
- AI 只读取监控端已经聚合和校验的指标；
- Web Console 不直连数据库；
- 浏览器不持久化管理员 Bearer token；
- API 对每个个人数据查询执行 org/team/role Scope 并写审计。

## 5. 执行协议

后续 Agent 必须遵守：

1. 只在本项目目录内工作。安装、登录项、浏览器 Host、注册表、注销、系统目录写入均需用户另行明确授权。
2. 从当前分支建立新的修复分支；不重写已有历史，不 force push。
3. 一次只领取一个 Task；先写能暴露当前断链的失败测试，再写最小实现。
4. 每个 Task 单独提交，提交信息包含 Task ID。
5. 不用“组件单测通过”替代 Runtime E2E。
6. Mac 结果不替代 Windows；模拟 Host 不替代真实扩展；Mock Provider 不替代 DeepSeek sandbox。
7. 所有测试只使用合成组织、员工、域名和事件。
8. 失败或未获授权的外部运行门禁写 blocked，不写 pass。
9. 每个 Task 汇报：
   - 修改文件；
   - 接通的数据流；
   - 新增的失败测试与修复后结果；
   - 验证命令和数量；
   - 未验证项；
   - delivery-status 允许变更的字段。
10. Reviewer 按配套的独立审核计划验收后，才能进入下一 Wave。

## 6. Gate 0：恢复可信状态

### Task G0-1：纠正状态与仓库卫生

目标：让文档和交付状态只表达已经有证据的事实。

主要文件：

- README.md
- docs/evidence/delivery-status.json
- docs/evidence/phase-1/*
- .gitignore
- tools/release-verifier

步骤：

1. 将 runtime_verified_mac 恢复为 unverified，或扩展状态模型加入 partial；现有状态模型只允许四值时应使用 unverified。
2. 更正 browser-native-messaging 文档：明确真实扩展未加载，输入帧为模拟。
3. 更正 Windows 文档：“模块存在”不等于“主路径完成”。
4. 删除 Git 索引中的 pycache / pyc，补忽略规则。
5. README 列出真实断链，并同步已存在的注册、认证和加密组件状态。
6. 增加证据索引，给每个 pass 指向具体命令、日期、平台和产物。

退出条件：

- 文档之间没有 pass / partial 矛盾；
- Git 不跟踪 target、dist、node_modules、pycache、pyc、数据库和真实日志；
- 没有证据的状态为 unverified 或 blocked。

### Task G0-2：建立失败的 Runtime 接线测试

目标：让现有 P0 不能继续被单元测试掩盖。

新增测试至少覆盖：

- Release Engine 从 enrollment config 恢复真实身份；
- enroll 成功后运行中的 Engine 立即生效；
- 重启后 token 和 queue key 可读取；
- 产品路径只能打开 encrypted LocalStore；
- Runtime flush 后真正调用 Uploader；
- Policy 获取、验签、pin、缓存和过期降级；
- Extension 真实输出可被 Host parser 接受；
- Windows transport 在 Windows target 编译；
- 首次未注册显示窗口，已注册 background 启动不显示窗口；
- UI 测试导入真实 main.ts，不复制 HTML；
- API policies 路由在 ESM runtime 可调用；
- enrollment 并发只能成功一次；
- activity/headers 不得成为免审计旁路。

退出条件：上述测试在修复前以正确原因失败，并记录失败输出。

## 7. Wave 1：被监控端真实闭环

### Task E1：建立唯一 AppRuntime 组合根

目标：将 Engine、Store、SecretStore、Policy、Uploader、Health 和 UI 状态放进同一个生命周期。

建议结构：

- src/runtime.rs：AppRuntime 与状态机；
- src/sync_loop.rs：上传、策略、健康调度；
- src/window.rs：首次窗口与按需状态窗口；
- commands.rs：只通过 Runtime API 修改状态；
- lib.rs：只负责 Tauri 组装与平台事件转发。

要求：

- AppRuntime 拥有单一 Engine 和单一 LocalStore；
- 启动先加载 enrollment，再决定是否采集；
- enroll 成功原子更新 config、secret、Engine identity；
- Runtime 暴露只读 HealthSnapshot 给托盘/UI；
- 采集线程、IPC、同步循环可控退出；
- 不再存在 agent-bin 与 Tauri 两套互相漂移的产品逻辑；thin-slice CLI 只能复用 Runtime 或移为测试工具。

测试：

- 未注册不入队；
- 注册后无需重启即可入队；
- 重启恢复身份；
- 双实例拒绝；
- shutdown flush 可验证且不挂起。

### Task E2：持久凭据与加密队列进入产品路径

目标：token、queue key 和事件在 Mac/Windows 重启后安全可用。

要求：

- macOS 使用 PlatformSecretStore / Keychain；
- Windows 完成 DPAPI 当前用户实现；
- queue key 独立于 device token；
- 所有产品入口调用 open_encrypted；
- 明文开发队列只允许测试 feature，Release 编译不可达；
- 写配置使用临时文件、fsync、原子 rename；
- 对旧开发明文队列执行明确清理或失败提示，不静默混用；
- SecretStore 失败时停止采集/上传并显示需要处理，不退回明文。

测试：

- SQLite 字符串扫描无事件 JSON、token、标题和域名；
- 重启可解密；
- 错 key 失败；
- DPAPI/Keychain adapter 合同测试；
- Release artifact 扫描不包含开发 token。

### Task E3：策略获取、固定公钥、缓存与应用

目标：Agent 只接受 enrollment 时固定的可信 key，并在离线时使用最后一个有效策略。

要求：

- enrollment 配置保存 signing key fingerprint 和 public key；
- key rotation 使用单独受审计协议，不能由普通策略响应替换 pin；
- 定义跨语言 canonical JSON；
- 每次策略响应验证 fingerprint、signature、issued_at、expires_at 和 version 单调递增；
- 缓存最后有效策略及获取时间；
- 过期后采用明确的 fail-safe 行为并上报；
- CollectionPolicy 更新进入同一个 Engine；
- 本地不可变硬规则始终优先，远端不能关闭。

测试：

- API Node 签名与 Rust 验签；
- API 重启后旧策略仍能验证；
- 篡改、降级、过期、错 key、未知字段全部拒绝；
- 离线缓存和恢复；
- hard drop 不能被管理员策略取消。

### Task E4：上传、精确 ACK、退避和健康闭环

目标：真实 Runtime 可持续上传，并向监控端提供可诊断事实。

要求：

- Uploader 使用真实 token 和 api_base_url；
- flush 与 upload 解耦，不能阻塞采集；
- 指数退避加抖动、上限和网络恢复唤醒；
- 401/403 停止重试风暴并进入 credential_error；
- 只删除 event_id 与 sequence 都匹配的 accepted；
- 非重试拒绝进入加密 quarantine；
- queue full 不得静默忽略，记录 health error；
- health 采集真实权限、自启动、最后上传、队列、隔离、策略状态；
- API 端使用严格 Health Schema 并提供 GET health read model。

测试：

- Runtime 进程级 enroll → collect fixture → upload → ACK → queue empty → DB row；
- 断网、500、429、partial ACK、401、sequence conflict；
- 进程强杀和重启恢复；
- health 不接受未知字段和无效时间。

### Task E5：首次配置窗口、后台行为与端侧 UX

目标：首次配置可完成，之后默认后台无窗口且状态真实。

要求：

- 未注册且非 background 启动时动态创建 setup 窗口；
- 已注册正常登录启动不创建窗口；
- background 参数有实际行为；
- 托盘“打开状态与诊断”按需创建/显示窗口；
- Setup 实现第 3.1 节五步流程；
- UI 通过单一 get_runtime_status 命令获得事实；
- enroll 按钮防重复提交，错误映射为稳定 error code；
- autostart requires approval 时不得显示完成；
- 成功后等待首个 health/upload confirmation，超时显示“已注册，连接待确认”；
- status 菜单文字随 Runtime 状态更新。

测试：

- 导入真实 UI 模块；
- mock Tauri commands 的成功、失败、超时和重试；
- Playwright 浏览器态可测页面；
- Tauri debug 进程测试窗口数量；
- 键盘、焦点、ARIA、200% 缩放和浅/深色快照。

### Task E6：统一浏览器协议和跨平台 Native Host

目标：真实 Chrome/Edge 输出能进入同一 Engine，且私密/敏感数据三层拒绝。

要求：

- 在 packages/contracts 定义唯一 BrowserObservation Schema；
- background.js、Host parser、local IPC 由同一 fixtures 验证；
- 明确 activate、deactivate、get_policy、policy_snapshot、heartbeat；
- 扩展连接后主动获取策略；
- 当前 tab 在阻止域名时先正确关闭旧状态，不能留下 current 导致后续不更新；
- Agent IPC 返回语义 ACK，不把“任意非空响应”当成功；
- Mac 使用 0600 Unix Socket；
- Windows 使用当前用户 Named Pipe 和 ACL，不引用 UnixStream；
- Chrome 与 Edge 使用各自 manifest/registry 安装方式；
- 正式扩展 ID 未确定时保持分发状态 unverified。

自动 E2E：

- 启动 Runtime 测试夹具；
- 启动真实 Host 子进程；
- 用扩展实际编码消息；
- 验证 Agent 加密队列；
- 覆盖同 tab 导航、窗口失焦、后台 tab、Host 重启、Agent 后启动、私密模式。

真实浏览器安装仍需用户单独授权。

### Task E7：macOS/Windows 状态事件、自启动和恢复

目标：前台应用、idle、lock、sleep/wake 与自启动真正进入 Runtime。

macOS：

- 前台采集保持主线程调用；
- 接入 idle、锁屏、睡眠、唤醒公开通知；
- lock/sleep 关闭当前 segment；
- wake 后重新观察；
- SMAppService 状态真实读取并反馈 requires approval；
- .app 不在固定 /Applications 路径时有明确策略，不写死错误路径。

Windows：

- Runtime 启动 SetWinEventHook 消息循环；
- QueryFullProcessImageNameW 规范化稳定 app_id；
- GetLastInputInfo 形成 idle/active Observation；
- WTS/power 事件形成 lock/sleep/wake；
- 正确把 Task XML 写给 schtasks，或使用可靠 Task Scheduler API；
- 当前用户、LeastPrivilege、单实例和失败恢复；
- Windows target 的 Cargo check、test、bundle 在 Windows Runner 执行。

退出条件：

- 两平台分别有 build 状态；
- 未获系统修改授权时登录实测为 blocked；
- 任何 Mac 结果不改变 Windows 状态。

## 8. Wave 2：监控端 API 和数据库成为可靠前端后端

### Task M1：修复 enrollment、认证、RBAC 和审计

目标：所有身份、组织、团队和个人数据访问有唯一、可测试的 Scope。

要求：

- enrollment 使用 pool.connect 获得专用 client，在事务内 SELECT FOR UPDATE；
- UPDATE 带 used_at IS NULL 条件并检查 rowCount；
- device_label 持久化；
- device token 过期、吊销和轮换语义一致；
- 所有 device endpoints 使用统一 DeviceAuth；
- 所有 admin endpoints 使用统一 Scope service；
- Manager 只能读取授权团队；
- activity/headers 删除或执行与明细相同授权和审计；
- audit_logs 增加 org_id、actor_id、request_id；
- subject/team 主键使用 org 复合唯一或强制 org 条件；
- 登录限速、会话撤销、过期清理；
- Web Console 采用 BFF HttpOnly session，不在 localStorage 保存 token。

负向测试：

- 同 code 50 并发只有一个 201；
- 跨 org subject upsert、设备查询、个人明细、审计日志全部拒绝；
- Operator 不能读取行为；
- 无审计旁路；
- 过期 device credential 不能取策略或上报健康。

### Task M2：严格 Schema、最小存储和迁移体系

目标：服务端只存已允许字段，并可安全演进。

要求：

- Activity、Health、Policy、Browser contract 使用同一 JSON Schema 编译校验；
- 移除手写契约漂移，或用 cross-language fixtures 锁定；
- activity_segments.payload 不再直接 JSON.stringify 输入；
- 标题敏感列单独处理，默认查询不返回；
- health 存储 queue、quarantine、last_upload、permission、autostart、policy、error；
- migrations 增加 schema_migrations 表和事务；
- 关键 FK、org scope 索引、时间索引和幂等约束；
- 状态事件与活动区间查询有明确模型；
- 所有列表使用 cursor pagination、日期范围和最大窗口。

退出条件：数据库测试断言实际列和 payload 内容，不再只检查 row 数。

### Task M3：监控端可达 HTTPS 入口和配置

目标：Release Agent 能从公司受管设备安全访问监控端。

要求：

- API 内部仍可监听 loopback；
- 增加监控端 gateway / reverse proxy；
- enrollment、activity、policy、health 使用 HTTPS；
- 明确 dev HTTP 仅限 debug localhost；
- 配置公开 base URL、证书、端口和健康检查；
- 数据库不暴露到局域网；
- 配置启动时 fail fast，不使用生产默认密码；
- API、Worker、Web 的 readiness 分开。

本地 Runtime 验证只使用项目内合成配置；安装证书或改系统信任需单独授权。

### Task M4：前端 Read API 和稳定 DTO

目标：Web Console 不直接拼原始表，所有页面有明确 DTO 和错误语义。

新增最小接口：

| 页面 | 接口能力 |
| --- | --- |
| Session | login、logout、me |
| Overview | 时间范围、在线、覆盖、延迟、类别、数据缺口 |
| Teams | list、summary、trend、sample size |
| Subjects | list、summary、timeline、data quality |
| Devices | list、detail、latest health、diagnostic history |
| Enrollment | create code、status、revoke |
| Policies | list、preview、create、rollback、rollout status |
| Audit | cursor list、filters |
| Insights | list、detail、feedback |
| System | API/Worker/DB/Provider health |

统一错误：

- unauthenticated；
- forbidden；
- validation_error；
- not_found；
- conflict；
- data_not_ready；
- upstream_unavailable；
- internal_error；
- 每个响应带 request_id。

## 9. Wave 3：确定性 Worker 和数据质量

### Task W1：应用/网站分类

目标：先用确定性规则得到可复现分类，AI 不参与计时或基础分类事实。

要求：

- 规则版本化；
- app_id 和 registrable_domain 映射；
- unknown 保留；
- 管理员修正规则只影响后续或明确重算；
- 分类结果带 rule_id 和 rule_version；
- 敏感域名不进入 Worker，因为端侧/API 已拒绝。

### Task W2：日聚合、覆盖率和缺口

目标：生成 Web 与 AI 共同使用的唯一指标事实。

输出：

- subject_daily_metrics；
- team_daily_metrics；
- app/domain/category duration；
- active、idle、offline、unknown gap；
- data_coverage_ratio；
- upload_delay percentiles；
- source completeness；
- metric_id 和 calculation_version。

规则：

- 处理跨午夜和时区；
- 区间去重、裁剪、重叠检测；
- team 少于 5 人不生成可展示匿名对比；
- 设备离线不能解释成低使用；
- 作业幂等、可重跑、有 watermark。

### Task W3：Worker 调度与可运维性

要求：

- 明确队列或数据库 job table；
- heartbeat、last success、lag、retry、dead letter；
- 不与 API 进程共享不可控循环；
- 分类和聚合失败不影响 ingest；
- 合成数据回放和 golden metrics；
- Web 系统页可见 Worker 状态。

## 10. Wave 4：Web Console 用户体验闭环

### Task U1：应用骨架、设计系统和 BFF Session

建议技术：

- Next.js App Router；
- 服务端 BFF 调 API；
- HttpOnly、Secure、SameSite session；
- TypeScript DTO；
- 组件测试 + Playwright；
- CSS tokens，不在第一版引入过重图表库。

设计 tokens：

- 8px spacing grid；
- 正文最小 14px，数据表 13–14px；
- 可见 focus ring；
- 颜色不作为唯一状态信号；
- 状态色满足对比度；
- 动画尊重 prefers-reduced-motion；
- 中文优先，时间/时区统一。

### Task U2：登录、总览和数据质量

实现：

- 登录错误、过期会话和安全重试；
- 总览第 3.2.1 的信息结构；
- 所有数字显示时间范围和更新时间；
- 缺数横幅优先于行为结论；
- 图表提供表格替代和 tooltip 键盘访问；
- 空、错、加载、部分失败状态。

### Task U3：团队和人员

实现：

- Team 列表、筛选和趋势；
- 少于 5 人不展示匿名对比；
- Personal timeline 访问前确认审计；
- 时间线虚拟化与分页；
- no activity / offline / missing permission 分开；
- 标题按权限和策略延迟加载；
- 导出不在前端同步生成。

### Task U4：设备和 Enrollment

实现：

- 异常优先设备表；
- Health 详情和诊断时间线；
- 生成注册码一次性展示；
- copy 状态、倒计时、作废；
- revoke 二次确认和影响说明；
- 版本、策略 rollout 和队列异常筛选。

### Task U5：策略、审计和系统状态

实现：

- 策略 diff、Schema 表单、不可变硬规则；
- 保存前预览；
- 发布/回滚状态；
- 审计筛选和 request_id；
- 系统服务健康与 Worker lag；
- 任何运维页不暴露个人活动内容。

### Task U6：跨状态 UX 验收

必须覆盖：

- 360、768、1024、1440 宽度；
- 200% zoom；
- 键盘完整操作；
- VoiceOver 基本路径；
- 深色/浅色；
- 慢网、500、401、403、空数据、大数据；
- 10k 设备列表使用服务端分页；
- 中文长文本不溢出；
- 页面截图基线和关键流程 Playwright。

## 11. Wave 5：DeepSeek 分析只在监控端

### Task A1：Insight 输入输出契约

输入只允许：

- metric_id；
- 时间范围；
- 分类时长；
- 趋势；
- 覆盖率和缺口；
- 规则事实。

禁止：

- 原始窗口标题；
- 完整事件 payload；
- device token；
- URL path/query；
- 任何端侧 secret。

输出 Schema：

- summary；
- findings；
- evidence_metric_ids；
- recommendations；
- limitations；
- coverage；
- model metadata；
- validation result。

### Task A2：Provider Adapter、降级和校验

要求：

- Worker 内部 Provider 接口；
- Fake Provider 先行；
- DeepSeek key 只来自 Worker secret；
- JSON output；
- 超时、限速、重试、成本上限；
- 输出 evidence 必须存在；
- 数值与确定性聚合二次校验；
- 失败只影响 Insight，不影响采集、上传和基础统计；
- sandbox 未真实调用前保持 unverified。

### Task A3：Insight UI 与反馈

要求：

- 每条结论可展开证据；
- 显示数据周期、覆盖率和生成时间；
- 证据不足时不生成强结论；
- 有帮助 / 事实错误 / 建议不适用；
- 反馈写审计并进入后续评估；
- Manager、Auditor、Operator 默认不可见，除非后续明确变更产品决策。

## 12. Wave 6：真实 E2E、双平台和交付

### Task Q1：自动化全链路测试栈

一条命令在项目内启动：

- 隔离 PostgreSQL；
- migrations；
- API；
- Worker；
- Web；
- fake endpoint runtime；
- fake DeepSeek。

必须验证：

- Admin 登录；
- 建 subject/team；
- 生成 code；
- Agent enroll；
- 系统和浏览器 fixture；
- 加密 queue；
- 上传 ACK；
- Worker 聚合；
- Web 页面出现正确统计；
- Insight evidence；
- revoke 后上传失败；
- teardown 只清理本项目 test stack。

### Task Q2：Mac 与 Windows 平台门禁

自动 Runner：

- Mac cargo/test/Tauri build；
- Windows cargo/test/Tauri build；
- extension bundle；
- API/Worker/Web build；
- artifact verifier；
- secret and analysis boundary scan。

真实设备步骤会写系统状态，未获用户明确授权时必须 blocked：

- 安装；
- 登录项；
- Native Host；
- Chrome/Edge；
- 注销登录；
- 锁屏/睡眠；
- 8 小时运行；
- 更新/卸载。

### Task Q3：监控端容器、备份和恢复

要求：

- API、Worker、Web、Postgres、gateway；
- secret 不进镜像；
- health/readiness；
- 数据卷与备份；
- 恢复演练；
- schema upgrade/rollback；
- 日志 request_id；
- 数据库不对 LAN 开放；
- local_build_monitor 与 runtime_verified_monitor 分开更新。

### Task Q4：打包、更新和证据状态

分别记录：

- local_build_mac；
- local_build_win；
- local_build_monitor；
- local_package_mac；
- local_package_win；
- local_package_browser；
- runtime_verified_mac；
- runtime_verified_win；
- runtime_verified_monitor；
- deepseek_sandbox_verified；
- signed/notarized；
- remote release；
- update；
- Pilot；
- user installed。

任何一项不能由另一项推导。

## 13. 最终 Definition of Done

只有同时满足以下条件，才可称“工程版闭环”：

### 被监控端

- Release 首次配置可完成；
- 重启后身份、token、queue key 和策略恢复；
- 正常登录后台无窗口；
- 所有事件经过单一 Engine 和加密队列；
- Mac/Windows 系统状态均真实接入；
- Chrome/Edge 真实链路通过；
- 上传、ACK、离线恢复和健康闭环；
- Agent/扩展产物无模型、Prompt 和分析代码。

### 监控端

- HTTPS 可达；
- 注册、认证、RBAC、team scope 和审计通过负向测试；
- Worker 生成确定性分类、聚合、覆盖率和缺口；
- Web Console 完成总览、团队、个人、设备、策略、审计、系统状态；
- AI 只读聚合指标，结论有 evidence；
- Provider 失败不影响基础功能。

### 体验

- 首次配置不会给出虚假成功；
- 正常后台运行不打扰员工；
- 监控端所有页面有加载、空、错、缺数和无权限状态；
- 数据缺失不被解释成低效率；
- 没有个人排行榜；
- 关键流程通过键盘、缩放和可访问性验收。

### 证据

- 本机测试通过；
- Mac/Windows build 分开；
- Runtime、Package、Release、Install 分开；
- 未授权的系统动作保持 blocked；
- 配套独立审核计划给出 PASS；
- README、evidence 和代码事实一致。

## 14. 可直接交给实现 Agent 的首条指令

你只允许在“电脑监控软件”项目目录内工作。完整阅读本计划和《独立审核计划-进度接入用户体验与交付证据-v1.0.md》。先执行 Gate 0，不得直接开始 Worker、Web Console 或 DeepSeek。一次只做一个 Task，先补失败测试，再做最小修复，运行该 Task 的全部门禁，单独提交并汇报证据。不得把 crate、route 或页面存在当作真实链路完成；不得把 Mac 当 Windows、模拟扩展当真实扩展、Mock Provider 当真实 DeepSeek。任何会写入用户 Library、Applications、浏览器配置、Windows Registry/Task Scheduler、注销或安装的动作都必须先取得用户当次明确授权，未授权就标记 blocked。
