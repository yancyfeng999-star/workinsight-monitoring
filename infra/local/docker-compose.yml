version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: workinsight
      POSTGRES_PASSWORD: workinsight_dev
      POSTGRES_DB: workinsight
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
